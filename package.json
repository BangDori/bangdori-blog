{
  "name": "bangdori-blog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter user-web dev",
    "build": "pnpm --filter user-web build",
    "start": "pnpm --filter user-web start",
    "lint": "pnpm --filter user-web lint",
    "format": "pnpm --filter user-web format"
  }
}
