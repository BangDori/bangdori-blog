import { ExternalPost } from '@database/entities/external-post.entity';
import { Post } from '@database/entities/post.entity';
import { User } from '@database/entities/user.entity';

export const entities = [Post, ExternalPost, User];
